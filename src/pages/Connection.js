import React from 'react';
import Header from '../components/Header';

const Connection = () => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default Connection;